#include <libheart.h>
#include "../inc/defs.h"
#include "../inc/soundbank.h"
extern rom game;
extern player bird;
extern snd audio;
extern sram SaveFiles;

void InitializeMainGame()
{
	if(SaveFiles.Files[game.currentfile].StageOnMap < 3)
	{
		
	}
}